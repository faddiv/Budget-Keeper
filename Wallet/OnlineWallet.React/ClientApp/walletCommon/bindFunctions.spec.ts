import { bind } from "./bindFunctions";
/*
class Dummy extends Object{
    @bind
    test1() { }
    test2() { }
}

describe("First Test", () => {
    it("is running", () => {
        var dummy = new Dummy();
        expect(dummy.hasOwnProperty("test2")).toBeFalsy();
        expect(dummy.hasOwnProperty("test1")).toBeTruthy();
    });
});*/