import { CommandFocusDirective } from './command-focus.directive';

describe('CommandFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CommandFocusDirective();
    expect(directive).toBeTruthy();
  });
});
